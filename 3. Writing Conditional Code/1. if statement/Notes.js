if ( condition ) {
	// conditional code
	// ...
}

if ( a < 50 ) {
	alert("It's true!");
	// etc...
}
// = is not used to check equality between variables
// == is used instead
// Never use a single equal sign for checking values
// { } <- code blocks