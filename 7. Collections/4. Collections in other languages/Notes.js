// Java allows anything in any element of the array (mixed data)
	// Specific data only allows specific types of elements
// Fixed size(immutable)/Changing size arrays(mutable)
	// Can change/unchangeable numbers of elements in the array
// Associative Arrays
	//Instead of the using numbers to label elements in the arrays, something more meaningful